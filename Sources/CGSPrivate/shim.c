// SPM requires a compilation unit for a C target. All declarations live in the
// public header; the actual implementations are provided by SkyLight.framework
// and ApplicationServices at link time.
#include "CGSPrivate.h"
